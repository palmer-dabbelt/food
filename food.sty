\NeedsTeXFormat{LaTeX2e}[1995/12/01]
\ProvidesClass{food}
\usepackage{fullpage}
\usepackage{units}
\usepackage{SIunits}
\usepackage{nicefrac}
\usepackage{ifthen}
\usepackage{url}
\usepackage{graphicx}
\usepackage{grffile}
\usepackage{multicol}
\usepackage[pdfusetitle]{hyperref}

% Here's my own metadata
\author{Palmer Dabbelt}

\makeatletter
\hypersetup{pdfsubject=Cooking,pdfkeywords=food;cooking}
\makeatother

% There are some sections inside an enviornment, don't show those.
% This shows ``section'', but not ``subsection''.
\setcounter{tocdepth}{1}

% This environment should wrap all recipes
\newenvironment{recipe}[1]
{%
  \section{#1}%
  \begin{multicols}{2}%
}
{%
  \end{multicols}%
  \clearpage%
}

%\newcommand{\servings}[1]{\textit{Serves: #1}\vspace{1em}}
\newcommand{\servings}[1]{}

% Here's where you stick photos of the food.  Note that this is only
% going to work when inside a recipe as it assums it's inside a
% multicols.
\newenvironment{images}
{%
  \begin{center}
}
{%
  \end{center}
}

\newenvironment{image}
{}
{}

% This wraps the ingredient list.  You should use \ingredient for each
% item in the list because it's formatted as a table.
\newenvironment{ingredients}
{%
  \subsection*{Ingredients}%
  \begin{center}%
    \begin{tabular}{rl}%
}
{%
    \end{tabular}%
  \end{center}%
}

\newcommand{\ingredient}[3]
{#1 #2&#3 \\}

\newcommand{\ingredientsep}{&\\}

% This wraps the list of steps that must be performed in order to cook
% something.
\newenvironment{steps}
{%
  \vfill%
  \columnbreak%
  \subsection*{Procedure}%
  \begin{itemize}%
}
{%
  \end{itemize}%
}

% A list of special notes about some sort of food that I've cooked,
% things I should try again and such.
\newenvironment{notes}
{%
  \subsection*{Notes}%
  \begin{itemize}%
}
{%
  \end{itemize}%
}

% This is put after every table of contents
\newcommand{\contentsskip}{\clearpage}

% Here's some nice fractions
\newcommand{\half}{\nicefrac{1}{2}}
\newcommand{\quarter}{\nicefrac{1}{4}}

% This allows me to ``reference'' things that will only be in the
% book.
\DeclareUrlCommand\UScore{\urlstyle{tt}}
\newcommand{\bref}[1]{\UScore{#1}}
